# Leave empty to use the Vite dev proxy (recommended). In production set this
# to the API origin if the frontend is served from a different host.
VITE_API_BASE_URL=

# "false" calls the real backend; anything else runs frontend-only mocks.
VITE_MOCK_API=true
